<head>
    <link rel="stylesheet" href="index.css">
</head>
<style>
    .alert{
    position: absolute;
    right: 50px;
    bottom: 40px;
    
}
</style>

<div>
<div class="container">
    <h1 style="text-align: center; margin-top: 2rem;">Transmicion de video vigilancia</h1>

                {{!-- TRANSMICION DE VIDEO VIGILANCIA  --}}

    <img src="" style="display: block;margin: 30px auto;">
        <script>
            const img = document.querySelector('img');
            const WS_URL = 'ws:///192.168.1.121:8888';
            const ws = new WebSocket(WS_URL);
            let urlObject;
            ws.onopen = () => console.log(`Connected to ${WS_URL}`);
            ws.onmessage = message => {
                const arrayBuffer = message.data;
                if(urlObject){
                    URL.revokeObjectURL(urlObject);
                }
                urlObject = URL.createObjectURL(new Blob([arrayBuffer]));
                img.src = urlObject;
            }
        </script>

        {{!-- ########################################### --}}

        
</div>
{{!-- class="dropdown-item" href="https://api.whatsapp.com/send?phone=+52 418 132 6979 &text=Alerta solicito apoyo !!! "><img src="../img/alerta.png" width="30" height="30" 
        class="d-inline-block align-top" alt="" --}}

<form action="/mensaje" method="get">
    <input class="alert" type="submit" value="Alerta">
</form>

<form action="/verfotosalerta/{{usuarioid}}" method="post">
    <input  type="submit" value="Evidencia">
</form>

{{!-- <button class="alert" href="/mensaje" onclick="">ALERTA
</button> --}}
</div>



        